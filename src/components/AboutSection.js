import { useState } from "react"

const AboutSection = () => {
  const [checked, setChecked] = useState(false)
  return (
    <div>
      This is AboutSection
    </div>
  )
}

export default AboutSection