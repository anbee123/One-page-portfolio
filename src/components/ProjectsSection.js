import { useState } from "react"

const ProjectsSection = () => {
  const [checked, setChecked] = useState(false)
  return (
    <div>
      This is ProjectsSection
    </div>
  )
}

export default ProjectsSection